enum UserType { company, jobSeeker, student }

enum UserMode { jobSeeker, freelancer }

enum JobType {
  fullTime,
  partTime,
  contract,
  temporary,
  volunteer,
  apprenticeship,
}

enum WorkplaceType { onSite, hybrid, remote }

enum ApplicationStatus { pending, accepted, rejected }

enum NotificationType { job, course, message, system }

enum EducationLevel {
  highSchool,
  diploma,
  bachelor,
  master,
  phd,
}

enum ExperienceLevel {
  noExperience,
  junior,
  mid,
  senior,
}