import 'package:flutter/material.dart';

class CompanySignupScreen extends StatelessWidget {
  const CompanySignupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('CompanySignupScreen'),
      ),
    );
  }
}
