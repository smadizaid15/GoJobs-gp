import 'package:flutter/material.dart';

class CompanyCheckEmailScreen extends StatelessWidget {
  const CompanyCheckEmailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('CompanyCheckEmailScreen'),
      ),
    );
  }
}
