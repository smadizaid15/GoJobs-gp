import 'package:flutter/material.dart';

class StudentOtpScreen extends StatelessWidget {
  const StudentOtpScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('StudentOtpScreen'),
      ),
    );
  }
}
