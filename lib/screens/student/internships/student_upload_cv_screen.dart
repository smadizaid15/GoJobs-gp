import 'package:flutter/material.dart';

class StudentUploadCvScreen extends StatelessWidget {
  const StudentUploadCvScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('StudentUploadCvScreen'),
      ),
    );
  }
}
