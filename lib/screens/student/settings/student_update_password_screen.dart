import 'package:flutter/material.dart';

class StudentUpdatePasswordScreen extends StatelessWidget {
  const StudentUpdatePasswordScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('StudentUpdatePasswordScreen'),
      ),
    );
  }
}
