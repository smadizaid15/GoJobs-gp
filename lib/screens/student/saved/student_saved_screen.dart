import 'package:flutter/material.dart';

class StudentSavedScreen extends StatelessWidget {
  const StudentSavedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('StudentSavedScreen'),
      ),
    );
  }
}
