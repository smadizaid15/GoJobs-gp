import 'package:flutter/material.dart';

class StudentFilterScreen extends StatelessWidget {
  const StudentFilterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('StudentFilterScreen'),
      ),
    );
  }
}
