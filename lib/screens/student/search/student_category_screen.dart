import 'package:flutter/material.dart';

class StudentCategoryScreen extends StatelessWidget {
  const StudentCategoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('StudentCategoryScreen'),
      ),
    );
  }
}
