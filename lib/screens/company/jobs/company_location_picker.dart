import 'package:flutter/material.dart';

class CompanyLocationPicker extends StatelessWidget {
  const CompanyLocationPicker({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('CompanyLocationPicker'),
      ),
    );
  }
}
