import 'package:flutter/material.dart';

class CompanyAddJobScreen extends StatelessWidget {
  const CompanyAddJobScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('CompanyAddJobScreen'),
      ),
    );
  }
}
