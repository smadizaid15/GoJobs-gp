import 'package:flutter/material.dart';

class CompanyEditJobScreen extends StatelessWidget {
  const CompanyEditJobScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('CompanyEditJobScreen'),
      ),
    );
  }
}
