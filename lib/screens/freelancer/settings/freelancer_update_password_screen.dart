import 'package:flutter/material.dart';

class FreelancerUpdatePasswordScreen extends StatelessWidget {
  const FreelancerUpdatePasswordScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('FreelancerUpdatePasswordScreen'),
      ),
    );
  }
}
