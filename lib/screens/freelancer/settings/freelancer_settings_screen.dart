import 'package:flutter/material.dart';

class FreelancerSettingsScreen extends StatelessWidget {
  const FreelancerSettingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('FreelancerSettingsScreen'),
      ),
    );
  }
}
