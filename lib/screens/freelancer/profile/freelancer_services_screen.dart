import 'package:flutter/material.dart';

class FreelancerServicesScreen extends StatelessWidget {
  const FreelancerServicesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('FreelancerServicesScreen'),
      ),
    );
  }
}
