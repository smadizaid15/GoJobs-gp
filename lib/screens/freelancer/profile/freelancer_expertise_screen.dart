import 'package:flutter/material.dart';

class FreelancerExpertiseScreen extends StatelessWidget {
  const FreelancerExpertiseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('FreelancerExpertiseScreen'),
      ),
    );
  }
}
