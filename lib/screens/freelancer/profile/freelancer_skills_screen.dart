import 'package:flutter/material.dart';

class FreelancerSkillsScreen extends StatelessWidget {
  const FreelancerSkillsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('FreelancerSkillsScreen'),
      ),
    );
  }
}
