import 'package:flutter/material.dart';

class JobseekerResumeScreen extends StatelessWidget {
  const JobseekerResumeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('JobseekerResumeScreen'),
      ),
    );
  }
}
