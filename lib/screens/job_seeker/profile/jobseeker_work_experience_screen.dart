import 'package:flutter/material.dart';

class JobseekerWorkExperienceScreen extends StatelessWidget {
  const JobseekerWorkExperienceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('JobseekerWorkExperienceScreen'),
      ),
    );
  }
}
