import 'package:flutter/material.dart';

class JobseekerLanguageScreen extends StatelessWidget {
  const JobseekerLanguageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('JobseekerLanguageScreen'),
      ),
    );
  }
}
