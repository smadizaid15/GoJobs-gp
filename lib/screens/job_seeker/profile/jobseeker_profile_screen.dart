import 'package:flutter/material.dart';

class JobseekerProfileScreen extends StatelessWidget {
  const JobseekerProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('JobseekerProfileScreen'),
      ),
    );
  }
}
