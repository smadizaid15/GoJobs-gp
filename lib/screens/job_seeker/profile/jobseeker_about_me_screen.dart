import 'package:flutter/material.dart';

class JobseekerAboutMeScreen extends StatelessWidget {
  const JobseekerAboutMeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('JobseekerAboutMeScreen'),
      ),
    );
  }
}
