import 'package:flutter/material.dart';

class JobseekerSkillsScreen extends StatelessWidget {
  const JobseekerSkillsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('JobseekerSkillsScreen'),
      ),
    );
  }
}
