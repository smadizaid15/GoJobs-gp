import 'package:flutter/material.dart';

class JobseekerSavedScreen extends StatelessWidget {
  const JobseekerSavedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('JobseekerSavedScreen'),
      ),
    );
  }
}
