import 'package:flutter/material.dart';

class JobseekerUploadCvScreen extends StatelessWidget {
  const JobseekerUploadCvScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('JobseekerUploadCvScreen'),
      ),
    );
  }
}
