import 'package:flutter/material.dart';

class JobseekerFilterScreen extends StatelessWidget {
  const JobseekerFilterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('JobseekerFilterScreen'),
      ),
    );
  }
}
