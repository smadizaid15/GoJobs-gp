import 'package:flutter/material.dart';

class JobseekerCategoryScreen extends StatelessWidget {
  const JobseekerCategoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('JobseekerCategoryScreen'),
      ),
    );
  }
}
