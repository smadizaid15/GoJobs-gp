import 'package:flutter/material.dart';

class JobseekerSearchScreen extends StatelessWidget {
  const JobseekerSearchScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('JobseekerSearchScreen'),
      ),
    );
  }
}
