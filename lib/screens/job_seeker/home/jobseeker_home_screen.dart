import 'package:flutter/material.dart';

class JobseekerHomeScreen extends StatelessWidget {
  const JobseekerHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('JobseekerHomeScreen'),
      ),
    );
  }
}
