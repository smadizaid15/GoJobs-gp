{
  "name": "gojobs-firebase-rules-tests",
  "private": true,
  "type": "module",
  "description": "Automated Firestore/Storage Security Rules tests, run against the Firebase Local Emulator Suite. Never targets the live project.",
  "scripts": {
    "test": "node --test --test-reporter=spec"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1"
  }
}
